apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - api.yaml
  - api-secret.yaml
  - api-service.yaml
  - db.yaml
  - db-secret.yaml
  - db-service.yaml
configMapGenerator:
  - name: nginx-config
    files:
      - config/nginx.conf